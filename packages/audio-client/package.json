{
  "name": "@hattama/audio-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser audio client for hattama.audio.v1 (AudioWorklet + resilient uploader). No dependencies.",
  "scripts": { "test": "node --test" }
}
